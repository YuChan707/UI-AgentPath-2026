# agent_prompts

Folder reserved for the backend **agent prompts** (audience, coaching, cultural,
feedback, vision, …) as versionable and reusable artifacts, separate from the code.

> **Status: empty (placeholder).** Today the audience pipeline prompts
> live as `PromptSpec` entities in
> [`../data_processor/prompts.py`](../data_processor/prompts.py). If the prompts of
> the agents in [`../backend/agents/`](../backend/agents/) get centralized here,
> document them in this README (one per file: purpose, input, expected output).
>
> This README also makes git track the folder (it does not version empty directories).
