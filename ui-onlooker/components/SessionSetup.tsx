// SessionSetup — persona + region + focus selector.
// Scaffold component. Wire to lib/store.ts (Zustand) and fill in the UI.

export function SessionSetup() {
  return (
    <div className="rounded border p-4">
      <h2 className="text-sm font-medium">SessionSetup</h2>
      <p className="text-xs opacity-60">persona + region + focus selector — scaffold</p>
    </div>
  );
}
