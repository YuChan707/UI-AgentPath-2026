// OutlookEmailCard — Outlook-styled email draft.
// Scaffold component. Wire to lib/store.ts (Zustand) and fill in the UI.

export function OutlookEmailCard() {
  return (
    <div className="rounded border p-4">
      <h2 className="text-sm font-medium">OutlookEmailCard</h2>
      <p className="text-xs opacity-60">Outlook-styled email draft — scaffold</p>
    </div>
  );
}
