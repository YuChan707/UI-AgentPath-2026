# Copy this file to .env and fill in your values
# NEVER commit .env — only commit this .env.example

DATABASE_URL=sqlite+aiosqlite:///./onlooker.db
# DATABASE_URL=postgresql+asyncpg://postgres:PASSWORD@db.PROJECT.supabase.co:5432/postgres
DATABASE_POOL_URL=

<<<<<<< HEAD
# AI / LLM — fully local Ollama (no cloud).
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.1
OLLAMA_EMBED_MODEL=nomic-embed-text
# data_processor transport (OpenAI-compatible endpoint of the same Ollama)
LLAMA_BASE_URL=http://localhost:11434/v1
LLAMA_MODEL=llama3.1
=======
# AI / LLM — default backend is local Ollama (llama3.1).
# Set LLM_BACKEND=groq to use Groq's OpenAI-compatible endpoint instead.
LLM_BACKEND=ollama
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=llama3.1
# Used by the Groq-based agents (vision, email) and when LLM_BACKEND=groq.
GROQ_API_KEY=your_groq_api_key_here
>>>>>>> 15f913d (cleaning and restructuring to microservices infrastructure)

# App
MOCK_MODE=false
ENVIRONMENT=development
CHROMA_OPEN_TELEMETRY__ENDPOINT=http://localhost:4317
CHROMA_OPEN_TELEMETRY__SERVICE_NAME=chroma-local
OTEL_EXPORTER_OTLP_HEADERS=
